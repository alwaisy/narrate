description = "Complete Reset: Clears all logs, temp data, drafts, posts, and reports."
prompt = "Execute a full project reset: Run 'bash main.sh reset'. Ensure all directories are recreated and empty."
